# Harshit Verma  Portfolio

Static portfolio site for Harshit Verma. Files included:

- `index.html`
- `styles.css`
- `script.js`
- `images/avatar.svg`

Quick local preview:

```powershell
# from this directory run
python -m http.server 8000
# then open http://localhost:8000 in your browser
```

Deploy: push to a GitHub repo and enable GitHub Pages (or use any static host).

Next steps:
- Replace placeholder text and project links with real content
- Add personal photo, resume, and social links
